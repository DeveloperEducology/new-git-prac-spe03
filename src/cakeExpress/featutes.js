// search feature at line in cakeHome page line 135

<TextInput
  style={styles.searchInput}
  placeholder="Search by pending or delivered"
  onChangeText={(text) => setSearchQuery(text)}
  value={searchQuery}
/>;
